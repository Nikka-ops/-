[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: hits real external services (nowcoder etc.); skip with -m "not live"
    slow: heavyweight local runs (real OCR models); skip with -m "not slow"
